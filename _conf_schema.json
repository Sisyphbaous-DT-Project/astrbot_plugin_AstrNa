{
  "fix_deepseek_v4_400": {
    "type": "bool",
    "description": "修复 DeepSeek v4 400 报错",
    "hint": "开启后，AstrNa 会同时处理两类 DeepSeek v4 400 报错：一是清理会触发接口 400 的异常 assistant 历史，包括空 assistant、只有 reasoning_content 的 assistant、以及只有 think 内容块的 assistant；二是兼容修复代理模型名下 DeepSeek v4 thinking mode 未回传 reasoning_content 导致的 400，适用于 deepseek-v4、deepseek-v4-pro、deepseek-v4-flash 及带代理前缀的同名模型。deepseek-chat、deepseek-reasoner 等旧版模型不会被误处理。默认关闭。",
    "default": false
  },
  "optimize_identity_metadata": {
    "type": "bool",
    "description": "优化身份元数据",
    "hint": "开启后，AstrNa 会把 AstrBot 自带身份识别注入的用户信息优化成 JSON 格式，并继续作为临时内容发送，不会写入会话历史。只有打开 AstrBot 自带的“用户识别/身份识别”按钮后，这个功能才会启用，不然开了也没用。默认关闭。",
    "default": false
  },
  "account_nickname_display": {
    "type": "bool",
    "description": "追加真实昵称",
    "hint": "开启后，AstrNa 会在支持的平台上额外注入账号真实昵称；取不到真实昵称、清洗后为空、或与群昵称相同时会自动跳过。只有打开 AstrBot 自带的“用户识别/身份识别”按钮后，这个功能才会启用，不然开了也没用。默认关闭。",
    "default": false,
    "collapsed": true,
    "condition": {
      "optimize_identity_metadata": true
    }
  },
  "account_nickname_only": {
    "type": "bool",
    "description": "仅使用真实昵称",
    "hint": "开启后，AstrNa 会优先把身份元数据里的 nickname 替换为账号真实昵称，不再同时提供群昵称；取不到真实昵称时回退到原群昵称。只有打开 AstrBot 自带的“用户识别/身份识别”按钮后，这个功能才会启用，不然开了也没用。默认关闭。",
    "default": false,
    "collapsed": true,
    "condition": {
      "optimize_identity_metadata": true,
      "account_nickname_display": true
    }
  },
  "group_member_identity_display": {
    "type": "bool",
    "description": "补充群成员身份",
    "hint": "开启后，AstrNa 会在 AstrBot 自带身份识别已生效时，通过 NapCat/aiocqhttp 补充当前发言人在群里的身份、群等级和专属头衔；只支持群聊和可查询成员信息的平台，查不到会自动跳过。不会写入会话历史。默认关闭。",
    "default": false,
    "collapsed": true,
    "condition": {
      "optimize_identity_metadata": true
    }
  },
  "birthday_info_display": {
    "type": "bool",
    "description": "注入生日信息",
    "hint": "开启后，AstrNa 会在 AstrBot 自带身份识别已生效时，通过 NapCat/aiocqhttp 的 get_stranger_info 尝试读取当前发言人或私聊对象的 QQ 生日月日，并写入临时身份元数据；只注入月日，不注入年份。查不到、字段为空或为 0 时会自动跳过，不会写入会话历史。默认关闭。",
    "default": false,
    "collapsed": true,
    "condition": {
      "optimize_identity_metadata": true
    }
  },
  "optimize_forward_nodes": {
    "type": "bool",
    "description": "优化合并转发",
    "hint": "开启后，AstrNa 会在 AstrBot 自带合并转发已经触发时，把过长的单个转发节点拆成多个较短节点。这是为了防止模型输出字符过多，触发 QQ 单条消息文字上限或 QQ/NapCat 单个合并转发节点隐藏限制而导致发送失败。只有打开并触发 AstrBot 自带的合并转发功能后，这个功能才会启用；AstrBot 原本不触发合并转发时，开了也没用。默认关闭。",
    "default": false
  },
  "forward_node_max_length": {
    "type": "int",
    "description": "单个转发节点目标长度",
    "hint": "合并转发内单个节点期望容纳的文本长度，达到该长度后会优先寻找句号、换行等自然断点，避免一句话被切得太碎。默认 1000。",
    "default": 1000,
    "collapsed": true,
    "condition": {
      "optimize_forward_nodes": true
    }
  },
  "forward_node_hard_limit": {
    "type": "int",
    "description": "单个转发节点硬上限",
    "hint": "合并转发内单个节点的最大文本长度。超过后一定会强制切开，用来避开 QQ/NapCat 对单条转发节点的隐藏限制。默认 1200。",
    "default": 1200,
    "collapsed": true,
    "condition": {
      "optimize_forward_nodes": true
    }
  },
  "optimize_long_reply_context": {
    "type": "bool",
    "description": "优化超长回复上下文",
    "hint": "开启后，AstrNa 会在 Bot 的 LLM 回复被合并转发或分段插件改写前后，尽量保留本轮回复的完整纯文本，让后续对话仍能看到 Bot 自己刚刚写过的长内容；只处理 Bot 自己的 LLM 回复，不展开群友发送的合并转发消息。默认关闭。",
    "default": false
  },
  "optimize_dynamic_system_prompt": {
    "type": "bool",
    "description": "AstrBot插件缓存优化",
    "hint": "开启后，AstrNa 会观察其他插件对 system_prompt 的注入内容；如果同一注入位置连续 3 次注入的动态内容都不同，并且能安全识别为纯追加或前置追加，就会持久化接管该注入位置，把后续完整动态语义块迁移到 extra_user_content_parts，并标记为临时内容，尽量减少动态 system prompt 对 LLM prompt cache 命中的影响。当前采用保守的段落/换行边界识别：固定不变的提示词会继续保留在 system_prompt，拿不准时会整体迁移动态追加内容，避免只迁移零散变化字符；复杂重写、删除或替换只记录日志并跳过。未来会继续改进语义块识别能力。默认关闭。",
    "default": false
  },
  "optimize_image_caption": {
    "type": "bool",
    "description": "更好的图像转述",
    "hint": "开启后，AstrNa 会在 AstrBot 使用独立图片转述模型时，保留用户设置的自定义转述提示词，并把用户当前问题和引用消息文本补充给转述模型，让转述模型带着问题看图，而不是只做全局描述。适用于主模型非多模态、但配置了 AstrBot 图片转述模型的场景；主模型本身支持图片，或 AstrBot 切换整次请求到多模态 fallback provider 时不会干预。默认关闭。",
    "default": false
  },
  "optimize_send_message_to_user": {
    "type": "bool",
    "description": "优化send_message_to_user工具",
    "hint": "开启后，AstrNa 会在普通聊天中拦截模型误调用 send_message_to_user 发送当前会话纯文本的情况，把这段文本改为普通最终回复，让发送前插件、表情包识别、分段回复、合并转发等正常生效。为保证这些发送前插件能命中，本模块会在普通聊天里提前关闭本轮流式输出；跨会话主动消息、定时任务、live、图片/文件/语音/视频/@ 等非纯文本消息不会接管，仍走 AstrBot 原逻辑。默认关闭。",
    "default": false
  },
  "provide_group_identity_tools": {
    "type": "bool",
    "description": "提供群身份查询工具",
    "hint": "开启后，AstrNa 会为 Bot 提供按需查询当前群成员身份、群等级、专属头衔、群主、管理员、群成员生日月日，以及当前群近期生日列表的工具；仅支持群聊和可查询成员信息的平台。这个功能不会主动注入到每轮上下文，只有模型需要查询群身份、群头衔、群等级、群主、管理员、某个群友生日或近期群友生日时才调用；不会查询跨群信息，生日不会返回年份。默认关闭。",
    "default": false
  },
  "optimize_reply_target_history": {
    "type": "bool",
    "description": "优化回复历史标记",
    "hint": "⚠️ 这个功能仍在研究中，请谨慎启用；如果发现模型混淆当前发言人、引用消息发送者或回复接收者，请立刻关闭。开启后，AstrNa 会在本轮请求里临时注入中文回复指向说明，明确当前发言人、被引用消息发送者，以及被引用 Bot 回复原本回复给谁，帮助模型不要把这些对象混淆。AstrNa 也会在发给模型前临时清理旧历史里的内部标记，并移除模型误输出的内部标记，避免污染真实发送内容和后续历史。真实发送到聊天平台的消息不会改变；旧历史不会被批量迁移，只做本轮请求内清理。默认关闭。",
    "default": false
  },
  "unlock_group_sender_concurrency": {
    "type": "bool",
    "description": "解锁群聊并发回复（实验性）",
    "hint": "⚠️ 实验性功能，请谨慎启用。AstrBot 默认会让同一个群或私聊里的 LLM 回复一条一条排队；开启后，AstrNa 会把群聊里的排队粒度改成“按群友分别排队”，让同一个群内不同群友可以并发触发 Bot 回复。同一群友连续发消息仍然串行，私聊仍然串行。此功能会调整消息处理时序；与消息防抖、分段回复、合并转发、主动消息或其他可能改写/延迟/取消消息流程的插件同时开启时请格外谨慎，若出现回复顺序、上下文或历史记录异常，请立刻关闭。本功能会尽量保留防抖插件的合并消息和取消旧回复逻辑，但无法保证兼容所有干扰消息流水线的插件。默认关闭。",
    "default": false
  }
}
